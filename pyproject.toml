[tool.poetry]
name = "django-vite-todo"
version = "0.1.0"
description = ""
authors = ["Swaraj <swaraj.dehuri@gmail.com>"]
readme = "README.md"
packages = [{include = "django_vite_todo"}]

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.2"
django-vite = "^3.1.0"
psycopg2 = "^2.9.12"
python-dotenv = "^1.1.0"
django-debug-toolbar = "^6.0"


[tool.poetry.group.dev.dependencies]
ipython = {version = "^9.1.0", python = ">=3.11"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
